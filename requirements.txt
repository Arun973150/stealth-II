# MIRAGE reproduction — core dependencies
# (CPU wheels work; a CUDA build of torch is strongly recommended for full runs)
torch>=2.2
torchvision>=0.17
open_clip_torch>=2.24
timm>=1.0
transformers>=4.40
sentencepiece>=0.2.0
protobuf>=4.25
pillow>=10.0
numpy>=1.26
tqdm>=4.66
